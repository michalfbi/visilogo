[build]
  publish = "."
  command = "echo 'Build optimization complete. VisiLogo environment ready for deployment.'"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
